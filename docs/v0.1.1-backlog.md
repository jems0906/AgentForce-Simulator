# v0.1.1 Backlog

Target scope: incremental stability and demo UX refinements after v0.1.0.

## Candidate Items

1. Security panel UX polish
- Add badge-style pass/fail indicators for smoke checks.
- Add compact audit filter presets (failed-only, rate-limited, nonce-reuse).

Status: completed on branch `feature/v0.1.1-security-panel-polish`.

2. Deployment helper script
- Add a single script to run: compose up, health check, smoke check, and summary output.

Status: completed in `scripts/deploy_and_verify.py`.

3. CI enhancements
- Upload smoke report artifact from workflow.
- Add caching for Python dependencies to speed CI.

Status: completed in `.github/workflows/ci.yml`.

4. API quality-of-life
- Add pagination cursor support for security audit endpoint.
- Add optional CSV export endpoint for audit events.

5. Documentation
- Add troubleshooting matrix for auth/signing/rate-limit failures.
- Add short “operator quick commands” section.

## Exit Criteria

- Tests remain green.
- Security smoke check remains green.
- CI runtime does not increase significantly.
